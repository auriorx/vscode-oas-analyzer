  {name}:
      description: |
        The response of `{name-no-postfix}`
      headers:
        x-correlation-id:
          $ref: "#/components/headers/x-correlation-id"
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/{schemaType}"